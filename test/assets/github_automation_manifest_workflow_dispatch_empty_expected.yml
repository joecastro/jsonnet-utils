name: CI

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  test:
    name: Test
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
